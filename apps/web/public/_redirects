# /api/* is handled by functions/api/[[path]].js, not here — Cloudflare
# Pages' 200-status _redirects rewrite only targets paths inside this same
# project, it cannot proxy to an external absolute URL (that's a
# Netlify-only feature). See that file for why the proxy exists at all.

# SPA fallback: React Router (Data Mode) client-side routes like /tasks/:id
# have no matching static file — serve index.html and let the router resolve
# them. Cloudflare Pages checks real static assets and Functions first, so
# this only catches paths with no matching file.
/*  /index.html  200
