import { describe, expect, it } from 'vitest'

// Scaffold smoke test — replaced by real unit tests from PH-04 onward.
describe('api scaffold', () => {
  it('vitest runs', () => {
    expect(true).toBe(true)
  })
})
